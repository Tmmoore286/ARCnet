This directory is intentionally empty in Git.

Use the manifests under `Ref-Packs/ARCnet-References/manifests/*.csv` and scripts under `Ref-Packs/ARCnet-References/scripts/` to download references locally for development or demos.

Do not commit downloaded files. The repo tracks manifests and scripts, not the binary artifacts.
